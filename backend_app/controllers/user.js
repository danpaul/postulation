var _ = require('underscore');

module.exports = function(options){

// this should get handled by sql login middleware now...
// asdf

	// var m = options.models;
	// var r = options.response;
	// var publicProps = ['id'];

	// this.create = function(options, callback){
	// 	m.user.create({}, function(err, user){
	// 		if( err ){
	// 			console.log(err);
	// 			return callback(null, r({errorCode: 'unknown'}));
	// 		}
	// 		return callback(null, r({data: {user: {id: user}}}));
	// 	});
	// }

	// *
	//  * Gets user data
	//  * @param  {int}  options.userId
	//  * @param  {bool}  options.public (defaults to true)
	 
	// this.get = function(options, callback){
	// 	var public = typeof(options.public) !== 'undefined' ? options.public : true;
	// 	m.user.get(options, function(err, user){
	// 		if( err ){
	// 			console.log(err);
	// 			return callback(null, r({errorCode: 'unknown'}));
	// 		}

	// 	});
	// }
}